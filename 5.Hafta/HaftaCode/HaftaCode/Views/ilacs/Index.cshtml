@model IEnumerable<HaftaCode.Models.Tablolarim.ilac>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ilacadi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SkTarihi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ilacadi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SkTarihi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Barkod }) |
            @Html.ActionLink("Details", "Details", new { id=item.Barkod }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Barkod })
        </td>
    </tr>
}

</table>
